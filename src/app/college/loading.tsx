import { PageLoader } from '@/components/ui/PageLoader';

export default function Loading() {
  return <PageLoader message="Loading College Portal" subMessage="Setting up your college workspace..." />;
}
