import { PageLoader } from '@/components/ui/PageLoader';

export default function Loading() {
  return <PageLoader message="Loading Super Admin Console" subMessage="Retrieving platform-wide data..." />;
}
