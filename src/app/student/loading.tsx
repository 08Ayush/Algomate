import { PageLoader } from '@/components/ui/PageLoader';

export default function Loading() {
  return <PageLoader message="Loading Student Portal" subMessage="Syncing your timetable and subjects..." />;
}
