# ============================================================
# Optimized Scheduler — PRODUCTION requirements (Railway)
# ============================================================
# Keep ONLY what the FastAPI server needs at runtime.
# Dev tools, docs, and testing packages are excluded to
# avoid OOM kills on Railway's starter plan.

# ── Core optimisation ────────────────────────────────────────
ortools>=9.7.0
numpy>=1.24.0
scipy>=1.10.0

# ── Machine learning (used by adaptive weight / bandit) ──────
scikit-learn>=1.3.0
pandas>=2.0.0
joblib>=1.3.0

# ── Database ─────────────────────────────────────────────────
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0

# ── Web framework ────────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
pydantic-settings>=2.0.0

# ── Async / HTTP ─────────────────────────────────────────────
httpx>=0.24.0,<0.29.0
aiohttp>=3.9.0

# ── Utilities ────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pyyaml>=6.0.0
cachetools>=5.3.0

# ── Logging & monitoring ─────────────────────────────────────
structlog>=23.2.0
psutil>=5.9.0

# ── REMOVED from production (add to requirements-dev.txt) ────
# supabase>=2.0.0          ← not used (replaced by psycopg2)
# redis>=5.0.0             ← optional, not needed
# pytest, pytest-*         ← testing only
# black, flake8, mypy      ← dev linting only
# pre-commit               ← dev only
# mkdocs, mkdocs-material  ← docs only
